IMPORTANT NOTE: this file is for random personal notes and should not be considered a formal
specification or documentation.  It is intended to capture ideas and thoughts during the development
process.  Most of it is likely to be outdated, irrelevant and or incorrect.
----------------------------------------------------------------------------------------------------

Three vectors:
    history of directories visited
        in reverse order: minus ('-')
        in forward order: plus ('+')
    most commonly visited directories
        comma (',')

New vector:
    parent directories of current directory
        dot dot ('..') or caret ('^')
        caret

#----------------------------------------------------------------------

=> cd --help
Usage:

  cdd [NAMED_OPTIONS] [FREEFORM_OPTIONS]

NAMED_OPTIONS consist of:

  --history               Show directory history depending on the direction
  --path=PATH_SPEC        Change to path specification (number or regular expression pattern)
  --direction={-|+|,}     Specify direction (backwards, forwards, most common) for history or PATH_SPEC
  --limit-backwards=n     Show at most n directories for last to first history
  --limit-forwards=n      Show at most n directories for first to last history
  --limit-common=n        Show at most n directories for most to least visited directories
  --path-separator=n      Force path separator to be a specific character
  --all                   Show all directories (overriding any 'limit' options)
  --action                Default freeform option to use when nothing else specified
  --gc                    Do garbage collection by minimizing directory stack
  --del=PATH_SPEC         Remove from history the directory matching PATH_SPEC
  --reset                 Reset the directory stack which clears all history
  --help                  Show help (this information)
  --version               Show version number

FREEFORM_OPTIONS:

  {-|+|,|?}?              Show directory history (backwards '-', forwards '+' or most common ',' or '?')
  {-|+|,|?}? n            Show history limited by n amount (n == 0 means show all history)
  PATH_SPEC               Change to PATH_SPEC using the default direction
  {-|+|,} PATH_SPEC       Change to PATH_SPEC using the specified direction

PATH_SPEC can be a number, a repeated direction, or a direction and a pattern.

See http://www.plan10.com/cdd for more information.
Copyright 2010-2021 Michael Graz

#----------------------------------------------------------------------

Simplified parameters spec

    deprecate --path-separator

    remove '?' as a placeholder for "common"

    rename --limit-backwards -> --max-backwards, -mb  (* intended for CDD_OPTIONS)
    rename --limit-forwards  -> --max-forwards,  -mf  (* intended for CDD_OPTIONS)
    rename --limit-common    -> --max-common,    -mc  (* intended for CDD_OPTIONS)
    use    --limit-upwards   -> --max-up,        -mu  (* intended for CDD_OPTIONS)

    add --max=n which applies to all directions
    the --all overrides any --max- options

    add --ignore-case which applies to pattern matching, default to true

    Rename --history to --list or -l
        works from default direction
        can add directory as a parameter: --list= --list=- --list=+ --list=.. --list=^

    --action - intended for CDD_OPTIONS environment variable when no parameters are given
    --direction - intended for CDD_OPTIONS environment variable when no direction is given

    PATH_SPEC always comes at the end
        like +2, -3, ,4, ..5, ^6, abc.*def

For --list with a pattern, filter the results by the pattern

#----------------------------------------------------------------------

process
    show_history
        process the pattern if it exists
    change_to_path_spec
        process the pattern if it exists

get_target
get_target_regex

#----------------------------------------------------------------------

Multiple options sets

CddOptionsForSetup
CddOptions - main options

When run from the command line with no stdin,
    present the init args

After that, split normal mode args into two parts
    environment options
    command line options

parse the environment options first
then parse the command line options

validate each time
for unrecognized environment options, warn and ignore
for unrecognized command line options, error and exit

#----------------------------------------------------------------------

Put in help for how to configure, like the zsh command for example

cd-deluxe needs to be configured.
Add the following to ~/.zshrc or else execute directly in the shell:

source <(/home/mike/base/cdd_v2/cd-deluxe/build1/main/cd-deluxe --init)

#----------------------------------------------------------------------

OKAY
check listing with direction shorthand
check fzf integration with direction shorthand
allow for more 'expand dots' ie "..1", "..2", ...

TODO
improve help
code cleanup - remove extraneous files
windows implementation
test fish shell
need to list minimum bash version, and perhaps other shells

#----------------------------------------------------------------------

When running cd-deluxe.exe from command line
    - look for cdd.cmd in current directory
    - if found
        doskey cd="....\cd-deluxe.exe" $*
    - else
        report on where to find it from github
        or, automatically create cdd.cmd in current directory
            with doskey command above

On windows cd-deluxe.exe needs to be run by the companion script cdd.cmd, which should exist in the same directory as cd-deluxe.exe.

- check if cdd.cmd does not exist in the current directory

#----------------------------------------------------------------------

CDD_VERSION and CDD_BUILD_NUMBER are set in the top level CMakeLists.txt

cdd/version.h.in

#----------------------------------------------------------------------

Need to remove
    install directory
    doc directory - or update

cleanup precompiled headers

review missing tests

#----------------------------------------------------------------------
To apply a tag and trigger the workflow:

# 1. Make sure you're on the right branch and it's up to date
git checkout main
git pull origin main

# 2. Create an annotated tag (recommended for releases)
git tag -a v1.0.0 -m "Release version 1.0.0"

# 3. Push the tag to trigger the workflow
git push origin v1.0.0

# 4. Verify the tag was pushed
git ls-remote --tags origin
